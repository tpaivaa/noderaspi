var Firebase = require('firebase');
var Ref = new Firebase('https://soittila.firebase.com/');


function updatetemps(sensors) {
	for ( i in sensors){
		Ref.
	}
};

module.exports = {
	updatetemps: updatetemps
}